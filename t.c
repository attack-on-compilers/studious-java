/* Type your code here, or load an example. */
// #include <stdio.h>
int square(int num) {
    // int z=100,y=1,x=2,w=3,v=4,u=5;
    // int z = x + num;
    // printf("Yo1 %d",z);
    // return z * num*u*v*w*x*y;
    return num*55;
}

int main()
{
    square(4);
    return 1;
}